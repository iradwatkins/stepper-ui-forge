# Production Environment Variables
# This file should be used for production deployments

# Supabase Configuration - PRODUCTION (Client-side safe)
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# PayPal Configuration - PRODUCTION
# CLIENT-SIDE (Safe to expose)
VITE_PAYPAL_CLIENT_ID=your-paypal-client-id-here
VITE_PAYPAL_ENVIRONMENT=production

# SERVER-SIDE ONLY (DO NOT use VITE_ prefix - these are secrets!)
PAYPAL_CLIENT_ID=your-paypal-client-id-here
PAYPAL_CLIENT_SECRET=your-paypal-client-secret-here
PAYPAL_ENVIRONMENT=production

# Square Configuration - PRODUCTION
# CLIENT-SIDE (Safe to expose)
VITE_SQUARE_APP_ID=your-square-app-id-here
VITE_SQUARE_LOCATION_ID=your-square-location-id-here
VITE_SQUARE_ENVIRONMENT=production

# SERVER-SIDE ONLY (DO NOT use VITE_ prefix - these are secrets!)
SQUARE_APPLICATION_ID=your-square-app-id-here
SQUARE_ACCESS_TOKEN=your-square-access-token-here
SQUARE_LOCATION_ID=your-square-location-id-here
SQUARE_ENVIRONMENT=production

# Cash App Configuration - PRODUCTION
# Note: Cash App Pay uses Square credentials - no separate client ID needed
# Cash App Pay is accessed through Square's Web Payments SDK

# Payment Webhook Configuration - PRODUCTION (Supabase Edge Function)
VITE_PAYMENT_WEBHOOK_URL=https://your-project-id.supabase.co/functions/v1/payments-webhook

# Google Maps Configuration - PRODUCTION
# CLIENT-SIDE (Safe to expose - restricted by domain and usage quotas)
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key-here

# Admin Configuration - PRODUCTION
# CLIENT-SIDE (Safe to expose - just an email address for admin identification)
VITE_ADMIN_EMAIL=your-admin-email@domain.com