# Production Environment Variables Template
# Copy this file to .env.production and fill in your actual values
# NEVER commit .env.production to version control!

# Supabase Configuration - PRODUCTION (Client-side safe)
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# PayPal Configuration - PRODUCTION
# CLIENT-SIDE (Safe to expose)
VITE_PAYPAL_CLIENT_ID=your-paypal-client-id-here
VITE_PAYPAL_ENVIRONMENT=production

# SERVER-SIDE ONLY (Keep these in your deployment platform's environment variables)
# DO NOT put these in any file that could be committed to git
# PAYPAL_CLIENT_SECRET should be set in Supabase Edge Function environment
# PAYPAL_ENVIRONMENT should be set in Supabase Edge Function environment

# Square Configuration - PRODUCTION
# CLIENT-SIDE (Safe to expose)
VITE_SQUARE_APP_ID=your-square-app-id-here
VITE_SQUARE_LOCATION_ID=your-square-location-id-here
VITE_SQUARE_ENVIRONMENT=production

# SERVER-SIDE ONLY (Keep these in your deployment platform's environment variables)
# DO NOT put these in any file that could be committed to git
# SQUARE_ACCESS_TOKEN should be set in Supabase Edge Function environment
# These should NEVER have the VITE_ prefix as they are server-only

# Cash App Configuration - PRODUCTION
# Cash App uses Square credentials - no separate configuration needed

# Payment Webhook Configuration - PRODUCTION
VITE_PAYMENT_WEBHOOK_URL=https://your-project-id.supabase.co/functions/v1/payments-webhook

# Google Maps Configuration - PRODUCTION
# CLIENT-SIDE (Safe to expose - restrict by domain in Google Console)
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key-here

# Admin Configuration - PRODUCTION
# This should be managed through database roles, not environment variables
# Remove any VITE_ADMIN_EMAIL configuration